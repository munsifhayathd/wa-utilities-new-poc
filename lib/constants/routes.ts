export const ROUTES = {
  // Auth routes
  LOGIN: '/login',
  
  // Main routes
  HOME: '/',
  JOBS: '/jobs',
  AUDIT_LOG: '/audit-log',
  REPORTS: '/reports',
  CONTRACT: '/contract',
  WORKER: '/worker',
  ASCON: '/ascon'
} as const; 